<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/toolbar_background"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:background="@color/colorPrimaryDark">

        <android.support.v7.widget.AppCompatImageView
            android:id="@+id/toolbar_icon_back_btn"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:paddingEnd="16dp"
            android:paddingStart="16dp"
            tools:ignore="ContentDescription"/>

        <ImageView
            android:id="@+id/toolbar_icon"
            android:layout_toRightOf="@id/toolbar_icon_back_btn"
            android:layout_toEndOf="@id/toolbar_icon_back_btn"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            tools:ignore="ContentDescription"/>

        <TextView
            android:id="@+id/toolbar_nav_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textColor="@color/colorAccent"
            android:textSize="16sp"
            app:fontPath="fonts/OpenSans/OpenSans-Semibold.ttf"
            tools:ignore="MissingPrefix"
            tools:text="Ололололо я тулбар"/>

        <ImageView
            android:id="@+id/toolbar_icon_additional_btn"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:background="@android:color/transparent"
            android:paddingEnd="16dp"
            android:paddingStart="16dp"
            android:visibility="gone"
            tools:ignore="ContentDescription"
            tools:visibility="visible"/>

        <ImageView
            android:id="@+id/toolbar_icon_second_additional_btn"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_toStartOf="@id/toolbar_icon_additional_btn"
            android:layout_toLeftOf="@id/toolbar_icon_additional_btn"
            android:background="@android:color/transparent"
            android:paddingEnd="16dp"
            android:paddingStart="16dp"
            android:visibility="gone"
            tools:ignore="ContentDescription"
            tools:visibility="visible"/>

    </RelativeLayout>

    <View
        android:id="@+id/toolbar_shadow"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@drawable/toolbar_shadow"/>
</LinearLayout>